# pnpm Workspace Configuration
# This file defines which directories contain packages in the monorepo

packages:
  - 'api'
  - 'web'
  - 'shared'

# Alternative pattern for larger repos:
# packages:
#   - 'apps/*'
#   - 'packages/*'
